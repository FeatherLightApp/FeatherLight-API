union ChannelResponse = ChannelPayload | Error

type ChannelPayload {
    open(
        active: Boolean
        inactive: Boolean
        public: Boolean
        private: Boolean
        peer: B64
    ): [Channel]!
    pending: PendingChannelsResponse!
    
}

