directive @auth(
    roles: [Role]! = [USER]
    actions: [Action]! = []
    use: UseType! = ACCESS
) on FIELD_DEFINITION


directive @limit(
    operations: Int!
    seconds: Int!
    limitKey: String! = "default"
) on FIELD_DEFINITION

#directive @GRPC(

#directive rest

# find place to put this and implement it
enum Action {
    ADD_INVOICE
    READ_BALANCE
}

enum UseType {
    ACCESS
    REFRESH
}