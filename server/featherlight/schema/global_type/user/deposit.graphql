type Deposit {
    address: String!
    amount: Int!
    confirmations: Int!
    blockhash: String
    blockindex: Int
    blocktime: Int
    txid: String!
    time: Int!
    timereceived: Int!
    comment: String
}
