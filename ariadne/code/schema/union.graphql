"""User response can resolve to either a User object or an error"""
union UserResponse = User | Error

union TokenResponse = TokenPayload | Error

union AddInvoiceResponse = AddInvoicePayload | Error

union PayInvoiceResponse = Invoice | Error

union Invoice = LocalInvoice | RemoteInvoice