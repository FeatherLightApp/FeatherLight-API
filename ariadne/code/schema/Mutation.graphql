type Mutation {
    createUser: UserResponse!
    login(user: String! password: String!): UserResponse!
    refreshAccessToken: UserResponse!
    addInvoice(memo: String! amt: Int!): AddInvoicePayload!
    payInvoice(invoice: String! amt: Int): PayInvoicePayload!
}
